#pragma once
#include <bits/stdc++.h>

class Utils
{
    /**
     * @brief Private constructor to avoid instantiation
     * 
     */
    Utils() {}

public:

    static const int INF = 1e9;

    static int square(int x)
    {
        return x * x;
    }
};